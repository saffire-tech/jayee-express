## 1. Hide products from stores without an active subscription\n\n- Add a SQL helper view / update RLS on `products` so the public `SELECT` policy also requires `EXISTS (SELECT 1 FROM stores s WHERE s.id = products.store_id AND s.subscription_expires_at > now())`. Owners and admins keep full access.\n- Update `stores` public read policy similarly so listings hide expired-subscription stores from buyers (owners/admins still see them).\n- Frontend already filters via these queries, so no extra client work — but add a small \