import { Button } from 'your-button-component'; // Adjust the import based on your project structure\n\nconst Navbar = () => {\n    const isSeller = profile?.current_mode === 'seller';\n    const buttonText = isSeller ? 'My Store' : 'Create Store';\n    const linkTo = isSeller ? '/seller' : '/create-store';\n\n    return (\n        <nav>\n            {/* Other navbar content */}\n            <Button link={linkTo}>{buttonText}</Button>\n        </nav>\n    );\n};\n\nexport default Navbar;\n